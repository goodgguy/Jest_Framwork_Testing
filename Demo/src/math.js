const add = (a, b) => a + b;
const subtract = (a, b) => b - a;
const multiply = (a, b) => a * b;
const divide = (a, b) => b / a;
module.exports =
{
    add: add,
    subtract: subtract,
    multiply: multiply,
    divide: divide
}